apiVersion: v1
kind: Service
metadata:
  name: backend-api
  namespace: default
  labels:
    app: backend-api
  spec:
   type: ClusterIP
   selector: 
     app: backend-api
  ports: 
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP